DATABASE_URL=postgresql+psycopg://vkr:vkr@db:5432/vkr
CORS_ORIGINS=http://localhost:3000

# AI
MISTRAL_API_KEY=
MISTRAL_CHAT_MODEL=ministral-8b-2512
MISTRAL_ALLOW_SMALL_FALLBACK=false
MISTRAL_EMBEDDING_MODEL=mistral-embed

DEMO_MODE=true

# Google Sheets: либо JSON одной строкой, либо путь к json-файлу внутри контейнера.
GOOGLE_SERVICE_ACCOUNT_JSON=
# Кому дать права редактора на созданную таблицу (обычно email администратора).
GOOGLE_SHARE_WITH_EMAIL=

# Локальная проверка смыслового сходства (CPU, без API)
LOCAL_EMBEDDING_ENABLED=true
LOCAL_EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
LOCAL_EMBEDDING_CACHE_DIR=/models/fastembed
